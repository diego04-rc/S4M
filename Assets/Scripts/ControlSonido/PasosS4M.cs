using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PasosS4M : MonoBehaviour
{
    private CharacterSoundManager characterSound;

    private void Awake()
    {
        characterSound = GetComponent<CharacterSoundManager>();
    }
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
